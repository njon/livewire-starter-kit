    <nav class="nav-menu">
        <div class="container">
            <div class="d-flex justify-content-center">
                <a href="#" class="nav-link">{{ __('Home') }}</a>
                <a href="#" class="nav-link">{{ __('Products') }}</a>
                <a href="#" class="nav-link">{{ __('Services') }}</a>
                <a href="#" class="nav-link">{{ __('About') }}</a>
                <a href="#" class="nav-link">{{ __('Contact') }}</a>
            </div>
        </div>
    </nav>